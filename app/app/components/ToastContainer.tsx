"use client";

/**
 * A placeholder component that replaces react-toastify's ToastContainer
 * This is a temporary solution until we implement a proper toast system
 */
const ToastContainerWrapper: React.FC = () => {
  // This is a placeholder component that doesn't render anything
  // In a real implementation, we would render a toast container
  return null;
};

export default ToastContainerWrapper;
